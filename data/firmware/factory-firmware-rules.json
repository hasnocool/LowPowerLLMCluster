{
  "schema_version": 1,
  "policy": {
    "require_https_source": true,
    "generic_serial_decoding_allowed": false,
    "allow_unpublished_manufacturer_rules": false,
    "notes": "Rules must encode an explicit manufacturer-published relationship. Empty by default except documented inspection methods; do not reverse-engineer serial formats and present them as vendor facts."
  },
  "rules": [
    {
      "id": "asrock-default-bios-chip-sticker-method",
      "enabled": true,
      "manufacturer": "ASRock",
      "model": null,
      "match_kind": "factory_bios_label",
      "factory_bios_version": "__READ_FROM_PHYSICAL_BIOS_CHIP_STICKER__",
      "source_url": "https://www.asrock.com/support/index.asp?cat=FindBIOS",
      "notes": "ASRock documents that the sticker on the BIOS chip contains the default BIOS version. This rule is a documented evidence method, not a serial decoder; callers must replace the sentinel with the observed sticker value before using it as exact evidence."
    }
  ]
}
