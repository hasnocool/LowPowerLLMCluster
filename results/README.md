# Benchmark results

This directory is for machine-readable benchmark result JSON produced by `llm-cluster-bench`.

Commit small, useful reference results with enough provenance to reproduce them. Keep very large raw traces outside git and link them from the result metadata when appropriate.

A result is only comparable with another result when workload class, model/model hash, quantization and workload dimensions match. The comparison command groups incompatible results separately.
